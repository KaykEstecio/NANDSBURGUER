{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "devCommand": "npm run dev",
  "env": [
    "DATABASE_URL",
    "JWT_SECRET",
    "JWT_EXPIRES_IN",
    "NEXT_PUBLIC_API_URL"
  ],
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}
